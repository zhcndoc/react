<Intro>

在 React 的压缩生产构建中，我们会避免传送完整的错误消息，以减少通过网络传输的字节数。

</Intro>

我们强烈建议在调试应用时在本地使用开发构建，因为它会跟踪额外的调试信息，并提供有关应用中潜在问题的有用警告；但如果你在使用生产构建时遇到异常，此页面将还原原始错误消息。

你刚刚遇到的错误的完整文本是：

<ErrorDecoder />

当你将一个 BigInt 值从 Server Component 传递给 Client Component 时，会发生此错误。
